/**
 * @file
 * Banner component JavaScript.
 */

((Drupal, once) => {
  'use strict';

  Drupal.behaviors.banner = {
    attach(context) {
      // Banner component JavaScript can be added here if needed
    }
  };
})(Drupal, once);
