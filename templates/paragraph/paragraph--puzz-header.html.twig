{#
/**
 * @file
 * Paragraph Puzz Header en su view mode específico.
 */
#}

{% if component is defined and props is defined %}
  {{ {
    '#type': 'component',
    '#component': component ?? 'puzz:header',
    '#props': props
  } }}
{% else %}
  {{ content }}
{% endif %}
